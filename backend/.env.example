# AWS Infrastructure Configuration
# -----------------------------

# AWS Account and Region
# ---------------------
# The AWS account ID where resources will be deployed
AWS_ACCOUNT_ID=908204759101
# The AWS region where resources will be deployed
AWS_REGION=ap-southeast-2

# Cognito Configuration
# -------------------
# Name for the Cognito User Pool
COGNITO_USER_POOL_NAME=swolept-user-pool
# Name for the Cognito App Client
COGNITO_APP_CLIENT_NAME=swolept-app-client
# Password policy for Cognito users
COGNITO_PASSWORD_MIN_LENGTH=8
COGNITO_PASSWORD_REQUIRE_UPPERCASE=true
COGNITO_PASSWORD_REQUIRE_LOWERCASE=true
COGNITO_PASSWORD_REQUIRE_NUMBERS=true
COGNITO_PASSWORD_REQUIRE_SPECIAL_CHARS=true
COGNITO_USER_POOL_ID=ap-southeast-2_r5ibADADi
COGNITO_USER_POOL_CLIENT_ID=5t4tmpolgpctgq3s7kav2dk0o1
# Free Tier Limit: 50,000 MAUs

# RDS Configuration
# ----------------
# Name for the RDS instance
RDS_INSTANCE_NAME=swolept-db
# Database name
RDS_DATABASE_NAME=swolept
# Database username
RDS_DATABASE_USER=postgres
# Database password (change in production)
RDS_DATABASE_PASSWORD=postgres
# RDS instance class (Free Tier: db.t3.micro only)
RDS_INSTANCE_CLASS=db.t3.micro
# Allocated storage in GB (Free Tier: 20GB)
RDS_ALLOCATED_STORAGE=20
# Multi-AZ deployment (false for Free Tier)
RDS_MULTI_AZ=false
# Free Tier Limit: 750 hours/month of db.t3.micro

# Database Connection
DATABASE_HOST=swolept-db.c5cqioyw2rsy.ap-southeast-2.rds.amazonaws.com
DATABASE_PORT=5432
DATABASE_NAME=swolept
DATABASE_USER=postgres
DATABASE_PASSWORD=RtvxDf_1991

# S3 Configuration
# ---------------
# Name for the S3 bucket
S3_BUCKET_NAME=swolept-uploads
# Enable versioning (false for Free Tier)
S3_VERSIONING=false
# Enable encryption (true for security)
S3_ENCRYPTION=true
# Free Tier Limit: 5GB storage

# API Gateway Configuration
# -----------------------
# Name for the API Gateway
API_GATEWAY_NAME=swolept-api
# Stage name for the API
API_STAGE_NAME=prod
# Enable CORS (true/false)
API_ENABLE_CORS=true
# Free Tier Limit: 1M API calls/month

# API Gateway Configuration
API_GATEWAY_NAME=swolept-api
API_STAGE_NAME=prod
API_ENABLE_CORS=true
API_ENDPOINT=https://mkjmoeewx5.execute-api.ap-southeast-2.amazonaws.com/prod/

# Lambda Configuration
# ------------------
# Lambda function timeout in seconds
LAMBDA_TIMEOUT=30
# Lambda function memory in MB (Free Tier: 128MB recommended)
LAMBDA_MEMORY=128
# Lambda function runtime
LAMBDA_RUNTIME=python3.9
# Free Tier Limit: 1M requests/month and 400,000 GB-seconds

# VPC Configuration
# ----------------
# CIDR block for the VPC
VPC_CIDR=10.0.0.0/16
# Number of public subnets
VPC_PUBLIC_SUBNETS=2
# Number of private subnets
VPC_PRIVATE_SUBNETS=2
# Enable NAT Gateway (false for Free Tier - expensive)
VPC_ENABLE_NAT=false

# Cost Control Settings
# -------------------
# Enable deletion protection (false for development)
DELETION_PROTECTION=false
# Enable resource cleanup on stack deletion (true for development)
ENABLE_CLEANUP=true
# Maximum budget alert threshold in USD
BUDGET_ALERT_THRESHOLD=1

# Tags
# ----
# Environment tag (dev, staging, prod)
ENVIRONMENT=dev
# Project tag
PROJECT=swolept
# Owner tag
OWNER=hugob